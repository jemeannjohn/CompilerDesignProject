#  Factorial using Recursion
fac(num){
  if(num == 1){
  	return 1;
  }
  y = 0;
  x = 0;
  z = 0;
  y = num - 1;
  x = fac(y);
  z = x * num;
  return z;
}
main(){
  x=10;
  z=0;
  z = fac(x);
  print "factorial of "; 
  print x;
  print "is :";
  print z;
}